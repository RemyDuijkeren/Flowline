# Residual Review Findings

From the `ce-code-review` pass on branch `feat/form-event-rename-resilience` (commit `f778f1b`), run `20260712-142328-9d025ff6`. These findings were reported by the review but did not meet the auto-apply bar (confidence 100, or 75 with cross-persona agreement, mechanical only) — they need human judgment before acting.

- **P2 (maintainability, confidence 75)** — ~~`FormEventRenameAdvisor.Suggest` and `FormEventReader.BuildFormNotFoundMessage` take a raw 5-field positional tuple `(Guid Id, string Name, string EntityLogicalName, string FormXml, string? RowVersion)` as a new cross-class API, even though `Flowline.Core.Models.DataverseForm` already models this exact shape. A positional tuple is an easy place for a future edit to transpose `Name`/`EntityLogicalName`.~~ **Resolved (2026-07-23)**: `FormEventReader` now builds `solutionForms` as `List<DataverseForm>` directly (not a tuple), `BuildFormNotFoundMessage` takes `IReadOnlyList<DataverseForm>`, and the tuple→record conversion that used to sit at the `Suggest` call boundary is gone. `Suggest` already took `IReadOnlyList<DataverseForm>`, so the whole path is now the named record end to end.

- **P2 (adversarial, confidence 75)** — ~~`FindSelfTagMatch` returns on the *first* sharing annotation that produces a self-tag match, without checking whether another sharing annotation (e.g. `onSave` vs `onLoad` on the same form pair) has its own genuine self-tag evidence pointing at a *different* candidate. `BuildSuggestion` then prints a rewrite line for every sharing annotation using that single winning candidate — so in a rare disagreement case, the tool could confidently tell a developer to repoint an annotation whose own evidence was never checked.~~ **Resolved (2026-07-23)**: took the precision design call. `FindSelfTagMatch` now has each sharing annotation vote for the candidate its own fingerprint tags and only returns a name when every evidence-bearing annotation agrees; on disagreement it abandons the self-tag signal (returns `null`) and lets the weaker signals hedge, rather than confidently repointing an annotation whose evidence pointed elsewhere. Regression tests added: `Suggest_TwoSharingAnnotationsBothSelfTagSameForm_EmitsStrongSuggestion` (agreement still fires) and `Suggest_TwoSharingAnnotationsSelfTagDifferentForms_AbandonsSelfTagSignalRatherThanGuess` (disagreement stays silent).

## Accepted, not a residual (documented tradeoff, no action needed)

- **P2 (adversarial, confidence 100)** — `FormEventIdentityCache.SetMany` does an unlocked read-modify-write; two concurrent `flowline push` runs against the same environment can clobber each other's cache writes. This is an already-accepted design tradeoff from the plan (`docs/plans/2026-07-12-001-feat-form-event-rename-resilience-plan.md`, KTD2): the cache is advisory bookkeeping, a lost write just means a future push re-resolves by name, and corrupt JSON already degrades to a clean cache-miss rather than crashing. Adding file locking would be disproportionate for this feature.
